# Strata Design & Interaction Patterns

This file accumulates design/interaction patterns that emerge from shipped work. It's a companion to `design-system.md` but for patterns that crystallize over time as agents build the app. The Director agent updates this file when it notices a pattern repeating across 3+ merged PRs.

## Patterns

*(No patterns yet. Entries will appear here as the Director promotes recurring observations from `docs/agents/director/scratchpad.md`.)*
